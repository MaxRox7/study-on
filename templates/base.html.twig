<!DOCTYPE html>
<html>
    <head>
        <title>Welcome to Symfony!</title>
    </head>
    <body>
        <h1>привет</h1>

        {% if user.isLoggedIn %}
            Hello {{ как дела? }}!
        {% endif %}

        {# ... #}
    </body>
</html>